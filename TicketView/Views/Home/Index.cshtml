@model TicketView.Controllers.HomeController.ViewModel
@{
    ViewBag.Title = "title";
    Layout = "_Layout.cshtml";
}

@foreach (var ticket in Model.Tickets)
{
    <div>
        <h2>@ticket.Title</h2>
        <h4>Created @ticket.Created.ToShortDateString()</h4>
        <p>#@ticket.Number [@ticket.Status]</p>
    </div>
}